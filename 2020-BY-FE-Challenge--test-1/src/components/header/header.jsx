import React from 'react';
import { AppBar, Toobar, IconButton, Badge, Menu, MenuItem, Menu, Typography } from "@material-ui/core";

const Header = () => {
    return (
        <div>
            <AppBar position="fixed" className={classe.AppBar} color="iherit">
                <toolbar>
                    <Typography>
                        <img src={} alt="commerce.js" height="25px" className={classes.image} />
                        <commerce className="js"></commerce>
                    </Typography>
                </toolbar>
            </AppBar>
        </div>
    )
}

export default Header;
